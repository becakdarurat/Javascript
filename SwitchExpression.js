// Digunakan untuk perbandingan data
let nilai = "D";
switch (nilai) {
    case "A":
        console.log(`Niali A`);
        break;
    case "B":
        console.log(`Nilai B`);
    case "C":
    case "D":
        console.log(`Nilai C`);
    default:
        console.log(`Niali default`);
        break;
}