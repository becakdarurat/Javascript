// For of mendapatkan nilai dari array dan object
const country = ["Indonesia", "Belanda", "Malaysia"];
for (const negara of country) {
    console.log(`${negara}`);
}

// For of di String
const str = `Sofyan Tanjung`;
for (const String of str) {
    console.log(`${String}`);
}

